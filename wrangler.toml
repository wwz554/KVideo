# Cloudflare Pages Wrangler Configuration
# Pages build output must point to the Vercel Build Output generated by next-on-pages.

name = "kvideo"
compatibility_date = "2026-08-12"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"

[vars]
# Set ADMIN_PASSWORD in the Cloudflare Pages dashboard for Production.
